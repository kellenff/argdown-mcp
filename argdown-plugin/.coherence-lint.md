# Sibling-coherence lint pass

## v0.2 (task 13) — 6 skills

All 4 rules passed on first inspection — no edits applied.

## v0.3 (task 9) — 7 skills (added dung-extensions; rebut-argument extended with Walton schemes)

All 4 rules still pass. 0 edits applied.

## Rule (a) — distinct lead verbs

Each description's lead verb (the first content verb after "...the user asks to..."):

| skill                     | verb      |
| ------------------------- | --------- |
| validate-argdown          | validate  |
| find-unsupported-premises | find      |
| trace-argument            | trace     |
| rebut-argument            | rebut     |
| extract-argument          | extract   |
| argdown-to-prose          | summarise |
| dung-extensions           | compute   |

All distinct. ✓

## Rule (b) — each SKIP names ≥1 sibling

Every description includes ≥2 `SKIP:` clauses naming sibling skills by name:

| skill                     | SKIPs                                                     |
| ------------------------- | --------------------------------------------------------- |
| validate-argdown          | find-unsupported-premises, argdown-to-prose               |
| find-unsupported-premises | validate-argdown, rebut-argument                          |
| trace-argument            | find-unsupported-premises, rebut-argument                 |
| rebut-argument            | find-unsupported-premises, validate-argdown               |
| extract-argument          | argdown-to-prose, validate-argdown                        |
| argdown-to-prose          | extract-argument, trace-argument                          |
| dung-extensions           | find-unsupported-premises, rebut-argument, trace-argument |

Every entry has ≥1 sibling. ✓

## Rule (c) — no verbatim user phrases duplicated

Quoted phrases per skill (each phrase appears in exactly one description):

- **validate:** `"validate this argdown"`, `"check argdown syntax"`, `"is this valid argdown"`
- **find-gaps:** `"find weak points in this argument"`, `"find gaps in the support"`, `"where is X grounded"`
- **trace:** `"trace why X follows"`, `"what supports Y"`, `"walk the argument chain"`
- **rebut:** `"rebut this argument"`, `"steelman a counter-argument to"`, `"how would someone disagree with"`
- **extract:** `"extract the argument from this prose"`, `"convert this to argdown"`, `"decompose this into premises and conclusions"`
- **argdown-to-prose:** `"summarise this argdown"`, `"explain this argument map in words"`, `"render this argdown as prose"`
- **dung-extensions:** `"compute the grounded extension"`, `"which arguments survive"`, `"are these arguments acceptable"`, `"find rejected arguments"`, `"find undecided arguments"`

No collisions. The `find rejected/undecided arguments` phrases in dung-extensions share the verb `find` with find-unsupported-premises' `find weak points / find gaps`, but the targets (rejected/undecided arguments vs. weak premises) are structurally distinct.

✓

## Rule (d) — cross-skill reference links resolve

All 7 SKILL.md files link only to `../references/{argdown-json-schema,argumentation-theory,mcp-tool-usage}.md` — all three target files exist under `argdown-plugin/skills/references/`. The theory reference now also covers Dung + Walton (added in task 8). ✓

## Result

All 4 rules pass on v0.3; **0 fixes applied**. Skills remain ready for the behavioural probe (still deferred from v0.2).

## Notes on potential cross-fire pairs (preemptive SKIPs)

Pairs flagged for monitoring during the behavioural probe (not currently breaking rule (c) but worth watching):

- **dung-extensions ↔ trace-argument** — both walk the argument graph, but `trace-argument` reports the path while `dung-extensions` reports a partition. Preemptive SKIP added in dung-extensions.
- **dung-extensions ↔ rebut-argument** — both reason about attacks, but `rebut-argument` proposes a counter while `dung-extensions` reports acceptability. Preemptive SKIP added in dung-extensions.
- **dung-extensions ↔ find-unsupported-premises** — both use `find` in user phrases, different targets. Preemptive SKIP added in dung-extensions.

If the behavioural probe shows cross-fire on these pairs, add reciprocal SKIPs in the OTHER direction.
